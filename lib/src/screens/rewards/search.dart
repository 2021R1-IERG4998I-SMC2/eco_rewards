import 'package:flutter/material.dart';

class RewardSearchScreen extends StatelessWidget {
  const RewardSearchScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Scaffold();
}
