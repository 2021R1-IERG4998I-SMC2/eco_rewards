import 'package:flutter/material.dart';

class RewardDetailScreen extends StatelessWidget {
  const RewardDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const Scaffold();
}
